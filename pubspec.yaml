name: flutter_initials
description: "A Package for generating initials from names."
homepage: "https://github.com/Safiul-Lathif/flutter_initials"
version: 1.0.2

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
